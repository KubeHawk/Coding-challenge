apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "crewmeister.fullname" . }}-dashboard
  namespace: {{ .Values.monitoring.namespace }}
  labels:
    grafana_dashboard: "1"
data:
  spring-boot.json: |
    {{ .Files.Get "dashboards/spring-boot.json" | nindent 4 }}